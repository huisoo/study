상위 클래스.. 하위 클래스( 추상적으로 정의)
음식 > 과일 > 배,사과

superclass, subclass



##
##배열에 쓰는 메소드는 prototype에 존재
##

인스턴스란 [1,2,3] : 실체

##소속메소드 확인, 공동체적 성향, 확인을 위한 것들, 클래스 자체에서만 접근 할 수 있는 것
- from(), isArray(), of() : static methods
- arguments, lenth, name  : static properties

##prototype 프로토타입 메소드
-concat()
-filter()
-forEach()
-map()
-push()
-pop()


##접근 가능 여부
static methods   _______Class________methods
static properties
              |위(x)               /위(O)
            instance
            
            
            
